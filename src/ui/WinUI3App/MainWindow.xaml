<Window
    x:Class="WinUI3App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="My Agent (PR 2 — WinUI 3 Shell Skeleton)">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title bar / command bar -->
        <controls:CommandBar Grid.Row="0" Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
            <AppBarButton Icon="Home" Label="Chat" Click="NavChat_Click"/>
            <AppBarButton Icon="View" Label="Suggestions" Click="NavSuggestions_Click"/>
            <AppBarButton Icon="Manage" Label="Dashboard" Click="NavDashboard_Click"/>
            <AppBarButton Icon="Setting" Label="Settings" Click="NavSettings_Click"/>
            <AppBarSeparator/>
            <AppBarButton Icon="Sync" Label="Test Transport" Click="TestTransport_Click"/>
        </controls:CommandBar>

        <!-- Main content area -->
        <Grid Grid.Row="1" Padding="16">
            <!-- Chat pane (default visible) -->
            <StackPanel x:Name="ChatPane" Visibility="Visible">
                <TextBlock Text="Chat (stub)" Style="{StaticResource TitleLargeTextBlockStyle}" Margin="0,0,0,8"/>
                <TextBox x:Name="ChatInput" PlaceholderText="Type a message to the agent..." KeyDown="ChatInput_KeyDown" Margin="0,0,0,8"/>
                <Button Content="Send (stub)" Click="SendChat_Click" Margin="0,0,0,12"/>
                <TextBlock Text="Conversation (local only, no backend yet):" Margin="0,0,0,4"/>
                <TextBox x:Name="ChatLog" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" Height="200" 
                         FontFamily="Consolas" Background="{ThemeResource SystemControlBackgroundAltMediumLowBrush}"/>
            </StackPanel>

            <!-- Suggestions pane -->
            <StackPanel x:Name="SuggestionsPane" Visibility="Collapsed">
                <TextBlock Text="Proactive Suggestions (stub)" Style="{StaticResource TitleLargeTextBlockStyle}" Margin="0,0,0,8"/>
                <ListView x:Name="SuggestionsList" Height="240">
                    <ListViewItem>📁 Review 12 new files in Downloads (high confidence)</ListViewItem>
                    <ListViewItem>📅 Calendar conflict tomorrow — suggest reschedule?</ListViewItem>
                    <ListViewItem>🔍 Research results ready for "Q3 budget model"</ListViewItem>
                </ListView>
                <Button Content="Refresh Suggestions (stub)" Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Dashboard pane -->
            <StackPanel x:Name="DashboardPane" Visibility="Collapsed">
                <TextBlock Text="Task Dashboard (stub)" Style="{StaticResource TitleLargeTextBlockStyle}" Margin="0,0,0,8"/>
                <TextBlock Text="No active tasks. Agent core not connected (PR 3)." Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                <TextBlock Text="History will appear here after PR 3." Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Settings stub -->
            <StackPanel x:Name="SettingsPane" Visibility="Collapsed">
                <TextBlock Text="Settings (stub)" Style="{StaticResource TitleLargeTextBlockStyle}" Margin="0,0,0,8"/>
                <CheckBox Content="Enable proactive suggestions (Downloads watcher)" IsChecked="True"/>
                <CheckBox Content="Prefer local models when possible" IsChecked="True" Margin="0,4,0,0"/>
                <TextBlock Text="Transport: Named Pipe + JSON (per IPC spike recommendation in docs/ipc-spike.md)" Margin="0,12,0,0" FontSize="12"/>
                <TextBlock Text="Model routing and full consents in PR 4+." Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Transport test result area (always visible at bottom of content) -->
            <Border Grid.Row="1" VerticalAlignment="Bottom" Background="{ThemeResource SystemControlBackgroundAltMediumBrush}" Padding="12" Margin="0,12,0,0">
                <StackPanel>
                    <TextBlock Text="Transport Status (PR 2 stub)" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TransportStatus" Text="Not connected. Click 'Test Transport' above." Margin="0,4,0,0"/>
                    <TextBlock Text="NamedPipeClientStream → \\.\pipe\my-agent-ipc (dummy ping)" FontSize="11" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="PR 2 skeleton — build requires .NET 8 SDK + Windows App SDK workload (see docs/setup.md). No functional backend." 
                   FontSize="11" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Padding="16,4" HorizontalAlignment="Stretch" TextAlignment="Center"/>
    </Grid>
</Window>
